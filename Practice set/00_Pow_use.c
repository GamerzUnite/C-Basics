#include <stdio.h>
#include <math.h> 
// this header file has "math" not "maths" to use
int main()
{
    int a, b;
    a = pow(3, 2); 
    //in the bracket 3 is the number the user is gonna or we are 
    // gonna give to the complier like if we gave 3
    
    // ,2 means The Power or its 3^2 or 3*3
    // this means the out put will be 9 
    
    printf("The Value nnow is %d\n", a);

    return 0;
}
